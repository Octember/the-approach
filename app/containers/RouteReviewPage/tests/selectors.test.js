// import { fromJS } from 'immutable';
// import { selectRouteReviewPageDomain } from '../selectors';

describe('selectRouteReviewPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
